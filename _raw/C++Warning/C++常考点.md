---
source: http://blog.csdn.net/lkz1156/article/details/51298027
---
\- 1.面向对象三大特征
\- 封装：在不影响其他部分的情况下修改或扩展被封装的变化部分，封装的作用解决了程序的可扩展性
\- 继承：子类继承父类，可以继承父类的方法和属性，实现了多态以及代码的重用，但是破坏了封装
\- 多态：接口的多种不同的实现方式即为多态
\- 3.const指针
\- const int\* p（int const\* p 相同的效果） 指向常量的指针 ，他指向的内容是const int类型，指向的内容不能变，指针可以变。
\- int\* const p 常量的指针 指针本身不能被改变
\- int const \* const p 指向(整形常量)的(常量指针)。指针和指针所指向的值都不能改变

\- 4.变量存储位置
\- 栈区：存放函数的参数值，局部变量的值
\- 堆区：由new，malloc等分配的内存块，如果程序员没有释放掉，在程序结束后操作系统会自动回收。
\- 全局变量区：全局变量和静态变量的存储是放在一起的（静态局部变量也保存在全局数据区）
\-

\- 5.static全局变量与普通全局变量有什么区别？
\- static全局变量只初始化一次，以防止在其他文件单元中被引用。
\-

\- 6.static局部变量和普通局部变量有什么区别？
\- static局部变量只被初始化一次，下一次依据上一次的结果值。
\-

\- 7.描述内存分配方式以及它们的区别
\- 1）从静态存储区域分配，内存在程序编译的时候就已经分配好了，这块内存在程序运行的整个期间都存在。例如全局变脸，静态变量
\- 2）在栈上创建。在执行函数时，函数内局部变量的存储单元都可以在栈上创建，函数执行结束时这些存储单元自动被释放。
\- 3）在堆上分配，也成为动态内存分配，程序在运行的时候用malloc或new申请任意的内存，用free或delete释放内存
\-

\- 8.引用使用的注意事项
\- 申明一个引用的时候必须要进行初始化，初始化之后不能再把该引用作为其他变量的别名，系统也不给引用分配存储空间。
\-

\- 9.引用作为函数参数有哪些特点
\- （1）传递给引用的函数与传递指针的效果是一样的
\- （2）使用引用传递函数的参数，在内存中并没有产生实参的副本，它是直接对实参操作
\- （3）使用一般的变量传递函数的参数时，当发生函数调用时需要给形参分配存储单元，如果传递的是对象，还
\- 将调用拷贝构造函数，使用引用比一般的变量传递参数的效率和空间都更好
\-

\- 10.引用作为函数返回值注意事项：
\- （1）不能返回局部变量的引用，局部变量在函数返回后会被销毁，引用变成空引用。
\- （2）不能返回函数内部new分配的内存引用，会造成new的空间无法释放
\- （3）可以返回类成员的引用
\-

\- 11.常引用
\- 利用引用提高程序效率，又要保护传递给函数的数据不在函数中被改变。
\-

\- 12.new,delete,malloc,free关系
\- new调用构造函数，完成动态内存分配和初始化功能。
\- delete通过调用析构函数，完成清理和释放内存工作的运算符
\- free不会调用析构函数，只释放内存
\- malloc与free是库函数，new与delete不是库函数
\-

\- 13.delete与delete\[\]区别
\- delete只会调用一次析构函数，delete\[\]会调用每一个成员的析构函数
\- delete与new配套使用，delete\[\]与new\[\]配套使用
\-

\- 14.子类析构时要调用父类的析构函数吗
\- 析构函数调用的次序是先派生类的析构后基类的析构，也就是说在基类的析构调用的时候，派生类的信息已经全部销毁了。定义一个对象时先调用基类的构造函数、然后调用派生类的构造函数，析构的时候恰恰相反，先调用派生类的析构函数、然后调用基类的析构函数。
\-

\- 15.虚函数与纯虚函数
\- 虚函数：在基类中冠以virtual的成员函数，提供一种接口，允许在派生类中对基类的虚函数重新定义。
\- 纯虚函数：在基类中为派生类保留一个函数的名字，一边派生类根据需要对它进行定义。作为接口而存在的纯虚函数不具备函数的功能，一般不能直接调用
\- 从基类中继承来的纯虚函数，在派生类中仍是虚函数，如果一个类中至少有一个虚函数，那么这个类被称为抽象类。
\-

\- 15.main函数执行以前还会执行什么代码？
\- 全局变量的构造函数会在main函数之前执行，为malloc分配必要的资源。
\-

\- 16.c++是不是类型安全的？
\- 不是，两个不同类型的指针之间可以强制转换。
\-

\- 17.float，指针类型的变量与‘零’的比较语句
\- float: const EXPRESSION EXP=0.000001
\- If(a<EXP&&a>-EXP)
\- pointer: if(a!=NULL)or if(a==NULL)
\-

\- 18.结构与联合有何区别
\- （1）结构与联合都是由多个不同的数据类型成员组成，但在任何同一时刻，联合中只存放一个被选中的成员（所有成员共用一块地址空间），而结构的所有成员都存在（不同成员的存放地址不同）。
\- （2）对于联合的不同成员赋值，将会对其它成员重写，原来成员的值就不存在了，而对于结构的不同成员赋值是互不影响的。
\-

\- 19.重载与重写(也叫覆盖)
\- 重载：是指允许存在多个同名函数，而这些函数的参数表不同（参数个数不同，或者参数类型不同，或许两者都不同）
\- 重写：是指子类重新定义父类虚函数的方法。
\-   和多态真正相关，当子类重新定义了父类的虚函数后，父类指针根据赋给它的不同的子类指针，动态调          用属于子类的该函数，这样的函数调用在编译期间是无法确定的(编译期间调用的子类函数的虚函数的地址无法给出)。因此，这样的函数地址是在运行期绑定的(晚绑定)。
\-

\- 20.什么情况下只能使用初始化列表，而不能用赋值
\- 1.const成员变量，因为成员变量只能初始化，不能赋值，所以必须放在初始化列表中。
\- 2.引用类型，引用必须在定义的时候初始化，并且不能重新赋值，所以也必须写在初始化列表中。
\- 3.没有默认构造函数的类类型，因为使用初始化列表可以不必调用默认构造函数来初始化，而是直接调用拷贝构造函数
\-

\- 21.const与#define相比，有何优点
\- const作用是定义常量、修饰函数参数、修饰函数返回值三个作用。被const修饰的东西都受到强制保护，可以预防意外的变动，能提高程序的健壮性。
\- 1）const常量有数据类型，而宏常量没有数据类型。编译器可以对前者进行类型安全检查。而对后者只进行字符替换，并且在字符替换可能会产生意料不到的错误。
\- 2）有些集成化的调试工具可以对const常量进行调试，但是不能对宏常量进行调试。
\-

\- 22.数组与指针的区别
\- 数组要么在静态存储区被创建（如全局数组），要么在栈上被创建。指针可以随时指向任意类型的内存块
\- 用sizeof可以计算出数组的容量，但是sizeof一个指针的话得到的是指针变量的字节数，c++没有办法知道指针所指内存容量。
\-

\- 23.int (\*s\[10\])(int)表示的是什么？
\- 定了一个函数指针数组，每个指针指向一个int func(int param)的函数。
\-

\- 24.指针与引用有什么区别
\- （1）引用必须被初始化，指针不必
\- （2）引用初始化之后值不能改变，指针可以改变所指向的对象
\- （3）不存在指向空值的引用，存在指向空指针的指针
\-

\- 25.基类的析构函数不是虚函数，会带来什么问题
\- 派生类的析构函数会用不上，会造成资源浪费
\-

\- 26.#include<file.h>与#include"file.h"的区别
\- 前者是从标准函数库的路径寻找和引用file.h，而后者是从当前工作路径搜寻并引用file.h
\-

\- 27.静态成员函数能不能同时也是虚函数？
\- 不能，调用静态函数不要实例，但调用虚函数需要从一个实例中指向虚函数表的指针已得到函数的地址，因此调用虚函数需要一个用例。
\-

\- 28.指针变量作为返回值
\- 局部变量的指针不能作为返回值为指针的函数的返回值，指向非局部变量的局部指针变量可以作为返回值
\- 返回指针只需要看这个指针指向的那段内存是不是在函数结束之后被销毁了就行，而不是看这个指针本身是什么属性。
\- example：
\- Char \*strcpy(char \*strDest,const char \*strSrc)
\- {
\- Char \*address=strDest;
\- Return address;
\- }
\- 因为strDest是从外部传进来的变量的，所以在函数结束时不会被销毁，所以程序正确运行
\- Char \*getString(void)
\- {
\- String p="123";
\- Char \*re=(char\*)result.c\_str()
\- Return re;
\- }
\- 因为result是局部变量，re指向局部变量，所以无法正确运行
\-

\- 29.static和const关键字作用
\- static：
\- （1）函数体内static变量的作用范围为该函数体，不同于auto变量，该变量的内存只被分配一次，因此其值在下次调用时仍维持上次的值。
\- （2）在模块内的static全局变量可以被模块内所用函数访问，但不能被模块外的其他函数访问。
\- （3）A函数内的static成员变量，在函数结束时并不被销毁，但是并不能被其他函数调用，当A函数再次被调用时，函数体内的static变量可以继续被使用，static变量的值仍是上一次退出函数时变量的值。
\- （4）在模块内的static函数只可被这一模块内的其他函数调用，这个函数的使用范围被限制在声明他的模块内
\- （5）在类中的static成员变量属于整个类所拥有，对类的所有对象只有一份拷贝。
\- （6）在类中的static成员函数属于整个类所拥有，这个函数不接收this指针，因而只能访问类的static成员变量。
\- const：
\- （1）欲阻止一个变量被改变，可以使用const关键字，在定义该const指针的时候，通常需要对他进行初始化，因为之后没有机会再去改变它。
\- （2）对指针来说，可以指定指针本身const，也可以指定指针所指的数据为const，或二者同时指定为const
\- （3）在一个函数声明中，const可以修饰形参，表明他是一个输入参数，在函数内部不能改变其值。
\- （4）对于类的成员函数，若指定其为const类型，则表明其是一个常函数，不能改变类的成员变量，常函数同时也不能调用该类中没有用const修饰的成员函数，只能调用常成员函数。
\- （5）对于类的成员函数，有时候必须指定其返回值为const类型，以使得其返回值不为左值
\-

\- 30.什么函数不能声明为虚函数，为什么？
\- 构造函数。
\- 虚函数采取一种虚调用的方法，虚调用是一种可以在只有部分信息的情况下工作的机制。如果创建一个对象，则需要知道对象的准确类型，因此构造函数不能为虚函数
\-

\- 31.父类写一个virtual函数，如果子类覆盖它的函数不加virtual，也能实现多态吗？
\- virtual修饰符会被隐形的继承，private也被继承，只是派生类没有访问的权限而已，virtual可加可不加，在子类的空间里有父类的所有变量（static除外），同一个函数只存在一个实体（inline除外），子类覆盖它的函数不加virtual也能实现多态，在子类的空间里，有父类的私有变量，但是私有变量不能直接被访问。
\-

\- 32.c++中产生的临时对象都是const
\-

\- 33.注意new int(12)和new int\[12\]的区别。
\- new int(12)是生成了一个值为12的int变量，new int\[12\]才是生成一个大小为12的数组
\-

    - int \*p\[4\];           //指针数组，是个有4个元素的数组，每个元素的是指向整型的指针（数组的每个元素
\-      都是指针）
\- int （\*p）\[4\];      //数组指针。它是一个指针，指向有4个整型元素的数组
\- int \*func(void)     //指针函数。无参函数，返回整型指针
\- int (\*func)(void)   //表示函数指针，可以指向无参，且返回值为整型指针的函数
\-

\- 函数指针和它指向的函数的参数个数和类型都应该是一致的；
\- 函数指针的类型应该与函数的返回值类型也必须是一致的。
\-

\- 35.c++默认构造函数何时被合成
\- 默认的构造函数是在编译器需要的时候被合成出来，而不是程序需要的时候；如果程序需要，则构造函数应由程序员实现。
\- 一句话总结什么时候生成默认构造函数：在逐位拷贝解决不了问题的情况下，就得合成构造函数
\- 遇到以下四种情况的时候，他需要为类合成一个默认的构造函数
\- （1）类的成员变量带有默认构造函数
\- （2）类的基类带有默认构造函数
\- （3）类带有virtual函数
\- （4）类带有一个virtual基类
\-

\- 36。c++类默认构造函数两个常见的误解
\- （1）任何class如果没有定义默认构造函数，就会被合成出来（只有上面四种情况才会被合成）
\- （2）编译器合成的默认构造函数，会显示设定class内每一个成员数据默认值（合成出来的默认构造函数，只会初始化基类子对象和成员类对象，其他的非static成员变量并不会被初始化）
\-

\- 37.构造函数何时被调用
\- （1）对一个对象做显示的初始化操作
\- （2）当对象被当作参数传递给某个函数时
\- （3）当函数返回一个类的对象时
\-

\- 38.默认的成员复制初始化
\- 如果class没有提供一个显示的复制构造函数，当class的对象以另一个对象作为初值进行构造时，其内部是以这样的方式完成的，对于基本的类型的成员变量。会使用位复制从一个对象复制到另一个对象，对于类类型的成员变量，会以递归的方式调用其复制构造函数
\-

\- 39.什么是c++对象模型
\- （1）语言中直接支持面向对象程序设计的部分：如构造函数、析构函数、虚函数、继承（单继承，多继承，虚继承），多态等
\- （2）对于各种支持的底层实现机制
\-

\- 40.c++中空类默认产生哪些类成员函数
\- 对于一个空类编译器产生4个成员函数
\- （1）默认构造函数
\- （2）析构函数
\- （3）拷贝构造函数
\- （4）赋值函数
\-

\- 41.结构是否可以有构造函数、析构函数以及成员函数，以及结构和类的区别
\- struct也可以有构造函数，析构函数，也可以继承甚至多重继承，
\- 区别是class默认访问控制是private，struct的默认访问控制是public
\- 目的是c++编译器兼容c开发的项目。
\-

\- 42.为什么要面向对象
\- 面向对象为了解决系统的可维护性，可扩展性，可重用性，封装解决了可维护性，继承解决了系统的重用性和扩展性，多态通过接口的方式实现了系统的可维护性，可扩展性。
\-

\- 43.面向对象与面向过程的区别
\- 面向过程适合处理简单的事情，直接关注流程，简单有效
\- 面向对象适合处理复杂的事情，先使用面向对象的方式对整体关系做出分类，然后，根据不同的类深入细节的处理
\-

\- 44.面向对象具体问题分析过程
\- 面向对象分析，面向对象的设计，面向对象的编程实现
\- （1）首先是需求分析，先不要思考怎么用程序实现它，先分析需求中稳定不变的客体都是些什么，这些客体之间的关系是什么
\- （2）把第一步分析出来的需求，通过进一步扩充模型，变成可实现的、符合成本的、模块化的、低耦合高内聚的模型。
\- （3）使用面向对象的实现模型。
\-

\- 45.虚基类概念及特点
\- 当一个基类被声明为虚基类后，即使它成为了多继承链路上的公共基类，最后的派生类中也只有它的一个副本，example：
\- Class CBase(){};
\- Class CDerive1:virtual public CBase(){};
\- Class CDerive2:virtual public Cbase(){};
\- Class CDerive12:public Cderive1,CDerive2(){};
\- 则在类CDerive12的对象中，仅有类CBase的一个对象数据。
\-

\- 虚基类特点：
\- 虚基类构造函数的参数必须由最新派生出来的类负责初始化(即使不是直接继承)
\- 虚基类的构造函数先于非虚基类的构造函数执行
\-

\- 46.头文件中ifndef/define/endif有什么用？
\- c++预编译头文件保护符保证即使文件被多次包含，头文件也只定义一次
\-

\- 47.sizeof的几点总结
\- sizeof是计算在栈中分配的内存大小
\- （1）sizeof不计算static变量占得内存
\- （2）使用sizeof计算结构或者类的大小时，静态成员不对结构或者类的大小产生影响，因为静态成员变量的存储位置与结构或者类的实例地址无关。
\- （3）没有成员变量的结构或者类的大小为一，因为必须保证结构或者类的每一个实例在内存中都有唯一的地址
\-

\- 48.悬垂指针，野指针，智能指针
\- 悬垂指针：指向曾经存在的对象，但是该对象已经不存在了，此类指针成为悬垂指针，结果未定义，往往导致程序错误，而且难以检测。
\-

\- 野指针：“野指针”不是NULL指针，是指向“垃圾”内存（不可用内存）的指针，if语句无法判断一个指针是正常指针还是野指针。
\- 野指针形成原因
\- （1）指针变量声明之后没有初始化，任何指针变量刚被创建时不会自动成为NULL指针，缺省值是随机的
\- （2）指针p被free或者delete之后，没有置为NULL，让人误以为p是个合法的指针，他们只是把指针所指的内存给释放掉，并没有干掉指针。
\- （3）指针操作超越了变量的作用范围
\-

\- 智能指针：
\- 智能指针是为了满足管理类中指针成员的需要，包含指针成员的类需要特别注意复制控制和赋值操作，原因是复制指针时只复制指针中的地址，而不会复制指针指向的对象，当类的实例在析构时，可能会导致悬垂指针的问题
\- 智能指针的一种通用实现技术是使用引用计数，智能指针类将一个计数器与类指向的对象相关联，引用技术跟踪该类有多少个对象共用同一个指针，智能指针结合了栈的安全性和堆的灵活性，本质上是栈对象内部包装一个堆对象。
\- 实现引用技术有两种经典策略：
\- （1）引入辅助类：缺点是每个含有指针的类的实现代码中都要自己控制引用计数，比较繁琐，特别是有多个这类指针时，维护引用计数比较困难。
\- （2）使用句柄类：
\-

\- 49.面向对象技术的基本概念是什么？
\- 类、对象、继承
\-

\- 50.公有继承，受保护继承，私有继承
\- （1）公有继承，派生类对象可以访问基类中的公有成员，派生类的成员函数可以访问基类中的公有和受保护的成员
\- （2）私有继承时，基类的成员只能被直接派生类的成员访问，无法再往下继承。
\- （3）受保护继承时，基类的成员也只被直接派生类的成员访问，无法再往下继承。
\- （4）共有继承时基类受保护成员，可以通过派生类对象访问但不能修改。
\-

\- 51.c++如何阻止一个类被初始化？一般在什么时候将构造函数声明为private？
\- （1）将类定义为抽象基类或者将构造函数声明为private
\- （2）不允许类外部创建类对象，只能在类内部创建对象
\- 51.c++如何阻止一个类被初始化？一般在什么时候将构造函数声明为private？

52.堆和栈
\- 　　从内存角度来说：栈区（stack）由编译器自动分配释放，存放函数的参数值，局部变变量的值等，其操作方式类似于数据结构中的栈，可静态亦可动态分配。
\- 　　堆区（heap）一般由程序员分配释放，若程序员不释放，可能造成内存泄漏，程序结束时可能由OS回收。只可动态分配，分配方式类似于链表。
\- 　　从数据结构角度来说：堆可以被看成是一棵树，如：堆排序。
　　而栈是一种先进后出的数据结构。

53.malloc和new
　　1.malloc与free是C++/C语言的标准库函数，new/delete是C++的运算符。但它们都可用于申请动态内存和释放内存。
　　2.对于非内部数据类型的对象而言，用maloc/free无法满足动态对象的要求。对象在创建的同时要自动执行构造函数，对象在消亡之前要自动执行析构函数。由malloc/free是库函数而不是运算符，不在编译器控制权限之内，不能够把执行构造函数和析构函数的任务强加于malloc/free，因此C++语言需要一个能完成动态内存分配和初始化工作的运算符new，和一个能完成清理与释放内存工作的运算符delete。
　　3.new可以认为是malloc加构造函数的执行。new出来的指针是直接带类型信息的。而malloc返回的都是void\*指针。new delete在实现上其实调用了malloc,free函数。
　　4.new 建立的是一个对象；malloc分配的是一块内存。
