---
source: http://www.nowcoder.com/test/question/done?tid=3791663&qid=25454#summary
---
1、值传递，引用传递，和指针传递。

2、||   和&&  的执行规则

3、前置和后置++，--等

4、那些需要用用初始化列表来初始化；
构造函数初始化时必须采用初始化列表一共有三种情况，
1.需要初始化的数据成员是对象(继承时调用基类构造函数)
2.需要初始化const修饰的类成员
3.需要初始化引用成员数据

5、sizeof（）  和  strlen（）的各种玩法；     

6、指针数组和数组指针；
int a\[4\] = { 1, 2, 3, 4 }; 
（1）数组名+数字n；取得是下标为n的元素的地址：
a+1 取得是第二个元素的地址，即2的地址；类型是int\*
（2）&a+1 跳过了整个数组得到了数字后面那个地址，类型为 int ( \* )\[ 4 \];  
要转换为int\*需要强制转换；
int\*ptr = (int \*)(&a + 1);  \*( ptr - 1 )的值即为 4 ；
（3）数组元素加一 p\[0\]+1；要根据p\[0\]的具体类型来确定；
如果p是一个指针数组；那么p\[0\]+1;取得就是p【0】那个地址的下一个地址；例：
int a\[\] = { 1, 2, 3, 4, 5 };
int\*p\[\] = { a, a + 3, a + 2, a + 1 };
p\[0\]+1 指向元素2的地址；p\[2\]+1指向的是4的地址；

5、#define 一个函数的使用：做法先将其全部展开然后进行计算即可；

6、double类型数据判断是否为0；

7、结构体大小计算

8、运算符的优先级 ： \*p++  ;   1<<3+2;   \*p\[10\];

9、时间复杂度和空间复杂度；

10、printf  和  cout   使用时：
int i =3 ；printf("%d %d", ++i, ++i，i++);
先算后面的++i；i变成4，再算前面的++i;i变成5；然后输出；
int   i = 1 ;    cout <<  ++i  <<  ++i <<  i++ <<  i++ <<   endl; 结果为 5 5 2 1 ；
编译器总是从后向前各项计算；对于前置++总是计算完了再给值，后置则直接给值；
==根本原因是前置++返回值为引用；后置++返回的是值；（猜的）；==

11、char \* str="hello world";
对于 str【2】=‘e’；\*（str+1）=‘e’的操作都是非法的 因为str的类型是 const char\*；

12、当一个类有基类，内部有一个其他类的成员对象，构造函数的执行顺序是
其他类的成员对象->基类->自己的成员；
动态的多态通过虚函数实现，基类指针指向派生类的对象，若指针调用的函数派生类中存在，且在基类中声明为虚函数，则调用的函数是派生类中的函数。

析构函数总是要声明为虚函数，这样析构时，先调用派生类的析构函数，再调用基类的析构函数，防止内存造成泄露

13、C语言中的未定义行为，即这些行为标准未做规定，由编译器决定。

## **1.同一运算符中多个操作数的计算顺序（&&、||、?和,运算符除外）**

例如：x = f()+g(); //错误

f()和g()谁先计算由编译器决定，如果函数f或g改变了另一个函数所使用变量的值，那么x的结果可能依赖于这两个函数的计算顺序。

## **2.函数各参数的求值顺序**

例如： printf("%d,%d\\n",++n,power(2,n)); //错误

在不同的编译器可能产生不同的结果，这取决于n的自增运算和power调用谁在前谁在后。

## **3.通过指针直接修改 const 常量的值**

直接通过赋值修改const变量的值，编译器会报错，但通过指针修改则不会，

14、不要把没用const修饰的常量不当常量：
           char \*p = "hello" ;
          p\[1\] = 'E';
这代码肯定崩溃；

15、关于栈和堆：

静态分配是指在编译阶段就能确定大小，由编译器进行分配，堆不可以进行静态分配，堆的申请都是在执行过程中进行的。

堆和栈的大小都可以设置，栈一般只有几KB。

堆在动态分配时，要申请连续的内存空间，释放后会产生碎片。

堆是使用malloc()、calloc()、realloc()等函数动态分配的，而使用alloca()函数可以动态分配栈的内存空间，释放的时候由编译器自己释放。

16.strcat  strcpy

17、new和delete：
