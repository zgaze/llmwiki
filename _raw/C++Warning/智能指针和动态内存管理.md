---
source: http://blog.csdn.net/fucangzxx/article/details/51534122
---
\_CrtDumpMemoryLeaks  windows下这个宏可以检测内存泄漏
C++标准库将shared\_ptr 、unique\_prt、 week\_ptr都放在了memory头文件中
智能指针（smart pointer）的意义：
他是一个类，封装了类似指针的操作；动态开辟出来的内存交由它管理，出了它的作用域会自动析构，这样就不用担心程序执行流在new和delete之间跳转了，更加容易更加安全的使用动态内存；

auto\_ptr:
1、两个auto\_ptr不能指向同一个对象；析构时重复析构会出错；因此不能有拷贝构造和赋值运算符重载；
2、内置类型的指针不能用等号，而应该用（）
3、析构函数用的是delete 因此不能管理数组；
4、不能用于STL标准库；
对于atuo\_ptr 一般多加一个 ower成员变量表示拥有权，没有拥有权就不能释放，在拷贝和赋值后将旧的owner设为false；

scoped\_ptr
不允许auto\_pt的拷贝和赋值；设为私有，并且只声明不实现；
unique\_ptr 
不支持拷贝和赋值，但是可以release和reset来将对象的所有权从一个unique\_ptr 转移向unique\_ptr ；
unique\_ptr <string> p2( p1.release()  );//release 将p1置为空,将所有权从P1 转移到P2
unique\_ptr <string> p3（new string（“”hello“”）；
p3.reset( p2 .release();)//所有权从P3转移到P2    ；reset释放了P2原来指向的内存
unique\_ptr 可以用来管理动态分配的数组，不定制删除器的shared\_ptr是不可以的；
unique\_ptr<int \[ \]> p( new int \[ 10\] );

最安全的分配和使用动态内存：
make\_shared<类型>（初始值）； 例如：shared\_ptr<int> p=make\_shared<int>（10）;
对于赋值和拷贝构造，shared\_ptr采用引用计数的方式来管理；
p1 = p ；那么递增p指向对象的引用计数；递减p1所指向对象的引用计算；如果p1 指向的对象引用计数为 0 则 释放；
返回shared\_ptr会给引用计数++；
shared\_ptr 如果要用了管理数组，必须等定制删除器 

1、引用计数更新存在着线程安全
问题1的解决需要对改变引用计数时加锁。
2、循环引用
问题2的解决使用weak\_ptr
\-    shared\_ptr<ListNode> prev; //语句1

\-     shared\_ptr<ListNode> next; //语句2

\-     prev->\_next = next;        //语句3

\-     next->\_prev = prev;        //语句4

经语句1、2之后prev的引用计数为1，经3、4后为2，但是最后两个对象均不能释放，因为prev的要释放的前提是next释放，而next的释放又依赖于prev的释放。最后就形成了循环引用，谁都释放不了

3、定置删除器，如果把一个file\* 类型的指针交给智能指针来管理，释放的时候如果还按普通方式来释放就会出错，
因此要定制删除器 也就是用仿函数，调用fclose吧文件指针关闭，而不是delete

weak\_ptr
weak\_ptr可以由shared\_ptr来构造；
boost::weak\_ptr只是提供了对管理对象的一个访问手段。
boost::weak\_ptr除了对所管理对象的基本访问功能（通过get()函数）外，还有两个常用的功能函数：
1\. expired() 用于检测所管理的对象是否已经释放；
2\. lock() 用于获取所管理的对象的强引用指针。
weak\_ptr只器监视作用；
由于弱引用不更改引用计数，类似普通指针，只要把循环引用的一方使用弱引用，即可解除循环引用。
weak\_ptr是为配合shared\_ptr而引入的一种智能指针来协助shared\_ptr工作，它可以从一个shared\_ptr或另一个weak\_ptr对象构造，它的构造和析构不会引起引用记数的增加或减少。没有重载\*和->但可以使用lock获得一个可用的shared\_ptr对象， 只需要让想被shared\_ptr管理的类从它继承即可，不能通过weak\_ptr来访问对象， 所以当要使用weak\_ptr指向对象的时候，必须先将它提升为普通智能指针，如果这时候对象已经被析构，则提升会失败报错。

C++动态内存管理：
new的实现分三步：
1、调用operator new或者 new【】  ，该函数分配一块足够大的，未命名的，原始的内存空间；
2、编译器运行相关的构造函数以构造对象，并为其初始化；
3、返回一个该对象的指针；
void \* operator new（size\_t size）
{
     if（void \*mem=malloc（size））
          return mem；
     else
          throw bad——alloc（）；
}
delete的实现：
1、对指针所指的对象或者数组中的元素执行析构函数，
2、调用operator delete的标准库函数释放内存空间；
void operator delete（void\*mem） noexcept
{
     free（mem）；
}
用户可以自定义 operator new 和 operator delete ，可以在全局定义，也可以在类内部将其定义为成员函数；
当编译器发现new或delete时；就会查找operator函数；
如果是类，先在类及其基类的作用域中查找；否则在全局区域查找；如果没找到用户自定义的才会使用标准库的版本；

定位new表达式：
new表达式，默认下把内存开辟到堆区。使用定位new表达式，可以在指定地址区域(栈区、堆区、静态区)构造对象，这好比是把内存开辟到指定区域。
定位new表达式调用 void \*operator new(size\_t, void \*); 分配内存。

placement new的作用就是：创建对象但是不分配内存，而是在已有的内存块上面创建对象。用于需要反复  创建并删除的对象上，可以降低分配释放内存的性能消耗。

“new (start) Screen;”   这是定位new操作

 Screen \*ps = new (start) Screen;   表示在已经开辟好的内存区start中为堆对象Screen申请一个内存
  也就是说，如果start占100个字节，你现在从它的第１个字节开始，往里面写入Screen，这样做今后要释放内存的时候有麻烦，如果Screen里面有指针，并开辟了空间，那么delete ps会调用析构函 数，然而在释放指针变量所占的内存空间时也释放了为对象所分配的内存空间，所以start中的那块内存也丢失了，因为ps也指向了start.所以要避免这种情况，就要先ps->~Screen()这样显示的调用Screen的析构函数，再delete \[\]start。

 定位new表达式，允许程序员将对象创建在已经被分配好的内存中，new表的式的形式如下：

new (place\_address) type
new (palce\_address) type (initializer-list)

place\_address必须是个指针，指向已经分配好的内存。

定位new表达式不能调用delete删除 placement new的对象，需要人为的调用对象的析构函数，并且人为的释放掉占用的内存。

malloc和free的底层实现
malloc 函数的实质是它有一个将可用的内存块连接为一个长长的列表的所谓空闲链表。
调用 malloc（）函数时，它沿着连接表寻找一个大到足以满足用户请求所需要的内存块。 然后，将该内存块一分为二（一块的大小与用户申请的大小相等，另一块的大小就是剩下来的字节）。 接下来，将分配给用户的那块内存存储区域传给用户，并将剩下的那块（如果有的话）返回到连接表上。
调用 free 函数时，它将用户释放的内存块连接到空闲链表上。 到最后，空闲链会被切成很多的小内存片段，如果这时用户申请一个大的内存片段， 那么空闲链表上可能没有可以满足用户要求的片段了。于是，malloc（）函数请求延时，并开始在空闲链表上检查各内存片段，对它们进行内存整理，将相邻的小空闲块合并成较大的内存块。
